= render partial: 'wiki_links', locals: { wiki: @wiki }
%h1 Update wiki "#{@wiki.title}"

= render partial: 'form', locals: { wiki: @wiki }

- if @wiki.private? &&  @wiki.user == current_user
  %h2 Colaborators
  = render(@colaborators) || 'No Colaborators'

  = render partial: 'colaborators/form', locals: {wiki: @wiki}
